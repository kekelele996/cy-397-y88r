<!-- 改编自：生活服务主题项目提示词/cy-397/cy-397.md；后端由 Spring Boot + MyBatis-Plus（Java 17）改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 项目概述
**项目类型：纯后端服务**
搭建一个合同模板生成与法律工单 API 服务，提供常用合同模板管理、合同填充生成、法律咨询工单提交与跟踪的后端接口。

### 功能需求

1. **合同模板管理**：管理合同模板库（租赁合同/劳动合同/借款合同/合作协议/保密协议），模板包含占位变量（甲方/乙方/金额/日期等）。
2. **合同生成**：用户选择模板并填入变量值，系统生成完整合同文本（纯文本/HTML 格式），支持导出 PDF（使用 wkhtmltopdf）。
3. **合同签署状态管理**：合同状态流转（草稿→待签署→已签署→已过期），记录签署时间和签署方信息。
4. **法律工单系统**：用户提交法律咨询工单（问题类型：劳动纠纷/合同纠纷/房产纠纷/知识产权/其他 + 问题描述 + 附件），分配工单状态。
5. **工单流转**：工单状态管理（待处理→处理中→已回复→已关闭），支持添加回复记录（文字 + 附件）。
6. **常见法律知识库**：管理法律FAQ 条目（分类 + 问题 + 解答），支持关键词搜索。
7. **用户合同库**：用户查看自己创建的所有合同列表，按状态筛选，支持合同模板收藏。

### 技术要求

- **后端框架**：Go 1.22 + Gin + GORM
- **ORM**：GORM（MySQL 驱动 `gorm.io/driver/mysql`）
- **数据库**：MySQL 8.0
- **认证**：JWT，使用 `github.com/golang-jwt/jwt/v5`
- **PDF 生成**：wkhtmltopdf（Docker 容器内安装）
- **代码规范**：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、合同状态枚举、工单类型）统一维护在 `backend/internal/constants/` 目录下；日志统一使用 Go 标准库 `log/slog`，通过中间件输出结构化日志；异常信息统一定义在 `backend/internal/constants/` 或 `backend/internal/dto/`，通过 Gin 全局错误处理中间件统一捕获并返回标准格式响应（`code/message/data`）。

## 代码存放规则

纯后端 API 服务，无前端代码。

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

后端目录结构建议（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go        # 程序入口：加载配置、初始化 DB/路由、启动 HTTP 服务
└── internal/
    ├── config/            # 配置结构体与加载逻辑（从环境变量读取）
    ├── model/             # GORM 模型：ContractTemplate、Contract、ContractSigner、LegalTicket、TicketReply、KnowledgeFAQ、TemplateFavorite 等
    ├── repository/        # 数据访问层：TemplateRepository、ContractRepository、TicketRepository、KnowledgeRepository、FavoriteRepository 等
    ├── service/           # 业务逻辑层：TemplateService、ContractService、TicketService、KnowledgeService、PDFService 等
    ├── handler/           # Gin HTTP 处理层：TemplateHandler、ContractHandler、TicketHandler、KnowledgeHandler 等
    ├── router/            # 路由注册：集中定义路由分组和中间件挂载
    ├── middleware/        # 中间件：JWTAuthMiddleware、CORS、请求日志、统一错误处理、参数校验等
    ├── dto/               # 请求/响应结构体与参数校验标签
    └── constants/         # 常量、错误码、合同状态/工单类型/工单状态枚举
├── templates/             # 合同模板文件
└── Dockerfile
```

数据库脚本放在 `database/init.sql`。
## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：
- 根目录 `docker-compose.yml`（编排 backend + MySQL 8.0）
- 根目录 `.env` 和 `.env.example`：包含 `COMPOSE_PROJECT_NAME=contractapi`、`DB_NAME`、`DB_USER`、`DB_PASSWORD`、`DB_HOST`、`DB_PORT`、`JWT_SECRET`、`BACKEND_PORT`
- `backend/Dockerfile`（Go 多阶段构建，并在运行镜像中安装 wkhtmltopdf）

backend/Dockerfile 使用 Go 多阶段构建，并保留 wkhtmltopdf：
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata wkhtmltopdf
WORKDIR /app
COPY --from=builder /app/server .
COPY templates/ ./templates/
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: contractapi`
- 容器名带 `contractapi-` 前缀
- backend 服务内部端口统一为 `8080`，对外映射 `${BACKEND_PORT:-19412}:8080`
- 数据库数据使用命名卷持久化
- 数据库配置 `healthcheck`，backend `depends_on` 等待数据库就绪
- MySQL 配置 `MYSQL_DATABASE`、`MYSQL_USER`、`MYSQL_PASSWORD`、`MYSQL_ROOT_PASSWORD` 等环境变量

## 构建验证清单（硬性约束）

- 后端必须能通过 `go mod tidy` 生成完整 `go.mod`/`go.sum`，依赖声明包括 `github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/golang-jwt/jwt/v5` 等
- 后端必须能通过 `go build ./...`；本地运行入口为 `go run ./cmd/server`
- 禁止将 handler、service、repository、model、middleware、dto、constants 职责合并到单一文件
- 不得使用 `version:` 字段，Compose V2 顶层声明 `name: contractapi`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目名称和一句话介绍、项目主要功能、快速启动方式（Docker Compose）、本地开发方式（`go mod tidy`、`go run ./cmd/server`）、技术栈（表格展示，后端写 Go 1.22 + Gin + GORM）、项目目录结构（代码块展示）、主要 API 列表、环境变量说明、License。

要求：Docker 部署命令放在最前面；告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   target/
   .env
   .DS_Store
   *.log
   ```
3. 执行 `git add -A && git commit -m "init: 项目初始化"`
